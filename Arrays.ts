/*
function sumArray(numbers: number[]): number {
    let sum = 0;
    for (let i = 0; i < numbers.length; i++) {
        sum += numbers[i];
    }
    return sum;
}

// Example usage
const array = [1, 2, 3, 4, 5];
const result = sumArray(array);
console.log(result); // Output: 15



function sumArray1(numbers: number[]): number {
    let sum = 0;
    for (let i = 0; i < numbers.length; i++) {
        sum += numbers[i];
    }
    return sum;
}

const array = [1, 2, 3, 4, 5];
const result = sumArray1(array);
console.log(result);






function sumArray2(numbers: number[]): number {
    let sum = 0;
    for (let i = 0; i < numbers.length; i++) {
        sum += numbers[i];
    }
    return sum;
}

const array = [1, 2, 3, 4, 5];
const result = sumArray2(array);
console.log(result);




function sumArray3(numbers: number[]): number {
    let sum = 0;
    for (let i = 0; i <numbers.length; i++) {
        sum += numbers[i];
    }
    return sum;
}

const array = [6, 7, 8, 9, 10];
const result = sumArray3(array);
console.log(result);




function sumArray4(numbers: number[]): number {
    let sum = 0;
    for (let i = 0; i<numbers.length; i++) {
        sum += numbers[i];
    }
    return sum;
}
const array = [11, 12, 13, 14, 15];
const result = sumArray4(array);
console.log(result);

*/
function sumArray5(numbers: number[]): number {
    let sum = 0;
    for (let i = 0; i <numbers.length; i++) {
        sum += numbers[i];
    }
    return sum;
}
const array = [16, 17, 18, 19, 20]
const result = sumArray5(array);
console.log(result);





